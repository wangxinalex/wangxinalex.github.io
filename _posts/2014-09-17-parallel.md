---
layout: post
title:  "Introduction to Parallel Programming"
date:   2014-09-16 20:39:39
categories: Parallel Booknote
tags: Parallel
---
##1.5 Shared-memory and distributed memory
1. Pthreads and OpenMP for shared memory system
2. MPI for diutributed memory system

##1.6 Concurrent , paralle , distributed

1. Concurrent: multiple tasks can be in progress at any instant
2. Parallel: multiple tasks cooperate closely to solve a problem
3. Distributed: A program may need to cooperate with other programs to solve a problem

###2.1.2 Process:
executable machine language
A block of memory: stack, heap
Discriptors
Security information
state of process

###2.2.5 ILP
two ways of ILP: pipelining, multiple issue
**Superscalar**: dynamic multiple issue
**Speculation**: Guess

###2.2.6 Hardware multithreading
Thread-level multithreading: coarser-grained parallelism
Hardware multithreading:
Simutaneous multithreading(SMT)

##2.3 Parallel hardware

###2.3.1 SIMD Systems
* Vector Processor
* Vector registers, vector instructions, interleaved memory, strided memory access amd hardware scatter/gather
* GPU

###2.3.2 MIMD Systems
Shared memory systems
Distributed memory systems
#Chapter 4 Programming with POSIX threads library

###4.2.3 starting the threads

<pre class = "brush:cpp">
	int pthread_create(pthread_t* thread_p, const pthread_attr_t* attr_p, void* (*start_routine)(void*), void* arg_p);
</pre>

###4.3.5 Stopping the thread

<pre class = "brush:cpp">
	int pthread_join(pthread_t thread, void** ret_val_p);
</pre>

##4.6 mutex

<pre class = "brush:cpp">
    int pthread_mutex_init(pthread_mutex_t* mutex_p, const pthread_mutexattr_t* attr_p);
    int pthread_mutex_destroy(pthread_mutex_t* mutex_p);
    int pthread_mutex_lock(pthread_mutex_t* mutex_p);//synchronized
    int pthread_mutex_trylock(pthread_mutex_t* mutex_p);//asynchronized–>EBUSY
    int pthread_mutex_unlock(pthread_mutex_t* mutex_p);
</pre>

##4.7 Semaphore #include <semaphore.h>

<pre class = "brush:cpp">
    int sem_init(sem_t* semaphore_p, int shared, unsigned initial_val);
    int sem_post(sem_t* semaphore_p);//++
    int sem_wait(sem_t* semaphore_p);//–-
    int sem_destroy(sem_t* semaphore_p);
</pre>

###4.8.3 Condition variables
A data object that allows a thread to suspend execution until a certain event or condition occurs

<pre class = "brush:cpp">
    int pthread_cond_signal(pthread_cond_t * cond_var_p);//unblock one of the blocked threads
    int pthread_cond_broadcast(pthread_cond_t * cond_var_p);//unblock all the blocked threads
    int pthread_cond_wait(pthread_cond_t * cond_var_p, pthread_mutex_t * mutex_p);//unlock the mutex and cause the executing thread to block until it is unblocked by another thread’s signal or broadcast
    pthread_mutex_unlock(&mutex_p);
    wait_on_signal(&cond_var_p);
    pthread_mutex_lock(&mutex_p);
</pre>

###4.9.3 Read-write lock

<pre class = "brush:cpp">
    int pthread_rwlock_init(pthread_rwlock_t* rwlock_p, const pthread_rwlockattr_t* attr_p);<
    int pthread_rwlock_rdlock(pthread_rwlock_t* rwlock_p);
    int pthread_rwlock_wrlock(pthread_rwlock_t* rwlock_p);
    int pthread_rwlock_unlock(pthread_rwlock_t* rwlock_p);
    int pthread_rwlock_destroy(pthread_rwlock_t* rwlock_p);
</pre>

##4.12 Summary
Three approaches to avoiding conflicting access to critical sections: busy-waiting, mutex, semaphore
