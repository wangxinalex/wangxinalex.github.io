---
layout: post
title: "SICP Note"
description: "SICP Book Notes"
category: Booknote Lisp
tags: Lisp
---
{% include JB/setup %}

#Chapter 1

##Chapter 1.1

**bound variable**: The procedure definition *binds* its formal parameters. The meaning of a procedure definition is unchanged if a bound variable is consistently renamed throughout the definition. If a variable is not bound, we say that it is *free*. The set of expressions for which a binding defines a name is called the *scope* of that name.

**lexical scoping**: The `x` gets its value from the argument with which the enclosing procedure `sqrt` is called. 

<pre class="brush:erl">
(define (sqrt x)
  (define (good-enough? guess)
    (< (abs (- (square guess) x)) 0.001))
  (define (improve guess)
    (average guess (/ x guess)))
(define (sqrt-iter guess)
    (if (good-enough? guess)
        guess
        (sqrt-iter (improve guess))))
  (sqrt-iter 1.0))
</pre>

> Ex1.2

<pre class="brush:erlang">
    (/ (+ 5
          4
          (- 2
             (- 3
                (+ 6
                   (/ 4 5)))))
       (* 3
          (- 6 2)
          (- 2 7)))
</pre>

> Ex1.3

<pre class="brush:erl">
    (define (min_two x y) 
        (if (< x y) x y))
    (define (min_three x y z)
        (min_two x (min_two y x)))
    (define (sum_of_larger_two x y z)
        (cond ((= x (min_three x y z)) (+ y z))
              ((= y (min_three x y z)) (+ x z))
              ((= z (min_three x y z)) (+ x y))))
</pre>

> Ex1.4

`(if (> b 0) + -)`该组合式的返回值是一个函数，如果组合式`(> b 0)`返回值为`#t`，则返回函数`+`，否则返回函数`-`。

> Ex1.5

**Normal Order（正则序）**: 先展开再归约，即惰性求值。

**Applicative Order（应用序）**: 先求值参数然后应用。

本例中如果采用应用序，那么会先求出组合式`(p)`的值然后陷入无限递归。如果采用正则序，会将表达式展开为`(if (= x 0) 0 (p))`，由于`if`的谓词部分返回为`#t`，所以会直接返回0，而不会对*<alternative>*部分进行求值。

> Ex1.6

<pre class="brush:erl; highlight: 4">
    (define (sqrt-iter guess x)
        (if (good-enough? guess x)
        guess
        (sqrt-iter (improve guess x)
                    x)))
</pre>

根据`P12`上关于if表达式的定义，只有当谓词为假时才回去求值`<alternative>`表达式，否则不会。而本例中使用if语句的前提是当谓词为真时结束递归，不再调用第4行的`sqrt-iter`递归。而`new-if`为普通函数，在解释器使用*应用序*求值时，`<then-clause>`和`else-clause`都会被求值，这会造成无限递归。

> Ex1.7

<pre class="brush:erl">
(define (sqrt2 x)
  (sqrt-iter2 1.0 0.0 x))

(define (sqrt-iter2 cur-guess pre-guess x)
  (if (good-enough2? cur-guess pre-guess)
      cur-guess
      (sqrt-iter2 (improve cur-guess x) cur-guess x)))

(define (good-enough2? pre-guess cur-guess)
  (< (abs (- pre-guess cur-guess)) 0.0001))

(define (average x y)
  (/ (+ x y) 2))
</pre>

> Ex1.8

<pre class="brush:erl">
(define (cubic x)
  (* x x x))

(define (cubic-root-iter guess x)
  (if (good-enough3? guess x)
      guess
      (cubic-root-iter (improve3 guess x) x)))

(define (good-enough3? guess x)
  (< (abs (- (cubic guess) x)) 0.001))

(define (improve3 guess x)
  (/ (+ (/ x (square guess)) (* guess 2)) 3))

(define (cubic-root x)
  (cubic-root-iter 1.0 x))
</pre>

##Chapter 1.2
